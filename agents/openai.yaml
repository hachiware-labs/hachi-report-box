interface:
  display_name: "Hachi Report Box"
  short_description: "Sync report sources into GitHub"
  default_prompt: "Use $hachi-report-box to register my report sources, sync their generated reports into my report box, and push the result to GitHub."
policy:
  allow_implicit_invocation: true
